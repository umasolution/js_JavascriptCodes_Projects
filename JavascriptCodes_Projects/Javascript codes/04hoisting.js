console.log(tipper(5));

function tipper(tip) {
    var bill = parseInt(tip);
    return (bill + 5);
}

var myName = "Himanshu";
console.log(myName);

var bigTipper = function (bigTip) {
    var bigBill = parseInt(bigTip);
    return (bigBill + 15);
}

console.log(bigTipper(100));