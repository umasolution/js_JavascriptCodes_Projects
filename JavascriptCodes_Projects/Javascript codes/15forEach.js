var states = ["Rajasthan", "Assam", "Maharashtra", "Kerela", 1947, "Delhi"];


// foreach works like a loop but actually it is a method used for arrays,
// but works exactly as loop.

states.forEach((s) => (console.log(s)));