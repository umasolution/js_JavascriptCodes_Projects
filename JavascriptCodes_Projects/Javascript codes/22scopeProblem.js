// var keyword is used widely in js but when js was used as it was never used like in complete mobile development, for backend and web development,
// var has some scope problem like when it is used in any scope then, it could be used outside that scope.

if(true) {
    var firstName = "Himanshu";
}

console.log(firstName);

// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    
// }